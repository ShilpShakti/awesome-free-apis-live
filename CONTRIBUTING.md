# योगदान कैसे करें (Contributing Guide)

इस रिपॉज़िटरी में नई API जोड़ना बहुत आसान है — बस एक YAML एंट्री जोड़िए और PR भेजिए,
बाकी सब ऑटोमेशन खुद संभाल लेगा।

## कदम-दर-कदम

1. `data/categories/` में सही कैटेगरी फाइल चुनें (या नई फाइल बनाएं अगर नई कैटेगरी है)।
2. नीचे दिए फॉर्मेट में अपनी API की एंट्री जोड़ें:

   ```yaml
   - name: "आपकी API का नाम"
     description: "एक लाइन में क्या करती है (हिंदी या इंग्लिश)"
     url: "https://example.com"
     test_endpoint: "https://api.example.com/health"   # ज़रूरी — इसी पर बॉट टेस्ट भेजेगा
     category: "India Public Data"                      # मौजूदा कैटेगरी नाम से मैच होना चाहिए
     auth: "None"                                        # None / API Key / OAuth आदि
     https: true
     cors: true
     rate_limit: "Open"                                  # जैसे "1000 req/day", "Key based"
     # दोनों नीचे वाले फील्ड्स वैकल्पिक (optional) हैं:
     # note: "कोई खास चेतावनी या जानकारी, जैसे 'unofficial mirror'"
     # skip_health_check: true   # सिर्फ localhost/self-hosted APIs (जैसे Ollama) के लिए — डेली पिंग स्किप हो जाएगी
   ```

3. PR खोलते ही **PR Validator** बॉट अपने आप आपके `test_endpoint` को टेस्ट करेगा और
   नतीजा कमेंट में डाल देगा।
4. अगर एंडपॉइंट 200 (या 401/403 अगर key चाहिए) रिटर्न नहीं करता, तो PR fail दिखेगा —
   पहले उसे ठीक करें, फिर push करें।

## नियम

- सिर्फ **मुफ्त** (कम से कम एक फ्री टियर वाली) APIs स्वीकार होंगी।
- `test_endpoint` कोई ऐसा URL हो जो बिना किसी सीक्रेट डेटा के भेजे भी रिस्पॉन्स दे
  (भले ही 401/403 हो) — ताकि डेली हेल्थ-चेक चल सके।
- डुप्लीकेट एंट्री न जोड़ें — पहले Ctrl+F से चेक कर लें कि वह API पहले से लिस्ट में नहीं है।
- एक PR में एक ही API जोड़ें, ताकि रिव्यू आसान रहे।

धन्यवाद! 🙏
